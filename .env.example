# Copy to .env and fill in for local development or Vercel production settings.

# --- Vercel & Production Next.js Environment ---------------------------
NEXT_PUBLIC_APP_URL=https://meirei.app
NEXT_PUBLIC_CHAIN_ID=196
NEXT_PUBLIC_XLAYER_RPC=https://rpc.xlayer.tech
NEXT_PUBLIC_EXPLORER_URL=https://www.oklink.com/xlayer
OTP_SIGNING_SECRET=meirei_live_security_hmac_secret_2026_chain196

# --- Meirei Live Environment (X Layer Chain 196) -----------------------
# Default execution wallet on X Layer (40-byte hex address)
MEIREI_WALLET=0x7f17d6224e7d48606598732c3f511412b5c1e922
# Chain selection: xlayer (196) or xlayer_test (1952)
MEIREI_CHAIN=xlayer
# 100% Live data: 0 for live Onchain OS execution, 1 for mock
MEIREI_MOCK_ONCHAINOS=0
# Web Dashboard & HTTP API
MEIREI_PORT=8787
MEIREI_HOST=127.0.0.1
MEIREI_CORS_ORIGIN=*
# Service Fee configuration
MEIREI_FEE_MODE=a2a-escrow
MEIREI_FEE_ASSET=USDG
MEIREI_FEE_ADDRESS=0x7f17d6224e7d48606598732c3f511412b5c1e922

# --- App ---------------------------------------------------------------
APP_BASE_URL=https://meirei.app

# --- Supabase ----------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_PASSWORD=
# Only needed to run migrations: psql $SUPABASE_DB_URL -f migrations/000N_*.sql
SUPABASE_DB_URL=

# --- Legacy Configurations ---------------------------------------------

# --- Twilio WhatsApp ---------------------------------------------------
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=
TWILIO_WEBHOOK_URL=
TWILIO_MENU_CONTENT_SID=
TWILIO_LIST_CONTENT_SID=

# --- Meta WhatsApp Cloud API -------------------------------------------
META_WHATSAPP_ACCESS_TOKEN=
META_WHATSAPP_APP_SECRET=
META_WHATSAPP_PHONE_NUMBER_ID=
META_WHATSAPP_VERIFY_TOKEN=
# Number users message, used for the "back to chat" deep link.
META_WHATSAPP_DISPLAY_NUMBER=

# Local dev only. Lets unsigned WhatsApp webhooks through so you can replay
# payloads with curl. Ignored outright when NODE_ENV=production.
WHATSAPP_ALLOW_UNSIGNED=

# --- Telegram ------------------------------------------------------------
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=
# Bare username, no leading @ (t.me/<name>). Used server-side to build the
# WhatsApp-to-Telegram account-link deep link.
TELEGRAM_BOT_USERNAME=
# Same value, exposed to the browser (NEXT_PUBLIC_ vars are inlined at build
# time) so the landing page's "Chat on Telegram" button has something to
# link to. Keep it in sync with TELEGRAM_BOT_USERNAME above.
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=

# --- sendam-ai ---------------------------------------------------------
SENDAM_AI_BASE_URL=
SENDAM_AI_SIGNING_SECRET=
# Local dev only. Logs full request/response bodies — which contain the
# literal text users type into WhatsApp. Ignored when NODE_ENV=production.
SENDAM_DEBUG=

# --- Send limits (OFF by default) --------------------------------------
# All three are unset, and unset means no limit. Users hated the caps, and a
# cap that stops someone paying their rent is a cap that gets the product
# uninstalled. What still applies unconditionally is the one nobody argues
# with: you cannot send money you do not have.
#
# Set any of them to re-impose it globally without a deploy — which is what
# these were env vars for. `off`, `none`, `unlimited` and `0` all mean off,
# so switching back has an obvious inverse.
#
# Per-user ceilings in tella_user_limits still apply either way, so a cautious
# user can opt into their own limit without it being imposed on everybody.
#
# Largest single transfer, in USDC.
TELLA_MAX_SEND_USDC=
# Rolling 24-hour per-user total, in USDC.
TELLA_DAILY_SEND_LIMIT_USDC=
# Above this, a send is authorized now and executed 24 hours later, giving the
# owner a window to notice and freeze. This is the most intrusive thing the
# product can do to someone and it lands on whoever is moving the most money,
# so it is off. It has its own switch rather than deriving from the cap above:
# re-imposing a cap during an incident should not silently reintroduce
# 24-hour delays as a side effect.
TELLA_HOLD_THRESHOLD_USDC=

# --- Cron + alerting ---------------------------------------------------
# REQUIRED for /api/cron/*. Vercel Cron sends it as a bearer token; the same
# value works for a manual curl. The routes refuse to run without it.
CRON_SECRET=

# Optional. Slack/Discord-compatible incoming webhook. Security events
# (forged webhook signatures, PIN lockouts, transfers of unknown outcome)
# POST here. Without it they degrade to structured console.error lines that
# a log drain can alert on.
ALERT_WEBHOOK_URL=

# --- Auth rate limiting (optional, defaults shown) ---------------------
# Attempts permitted per user per scope before lockout.
TELLA_AUTH_MAX_ATTEMPTS=5
TELLA_AUTH_WINDOW_SECONDS=900
TELLA_AUTH_LOCKOUT_SECONDS=900

# --- Admin dashboard ---------------------------------------------------
# Google subject ids allowed into /admin, comma-separated. Subs rather than
# emails, for the same reason tella_google_identity matches on sub: a
# Workspace address can be reassigned to a new person, and an allowlist keyed
# on one hands them the dashboard.
#
# An empty value means NOBODY, never everybody — a misconfigured env var must
# not open the door. To find a sub, sign in once and read the rejection line
# from the logs, or select google_sub from tella_google_identity.
ADMIN_GOOGLE_SUBS=
# Signs the admin session cookie (stateless, 4h absolute, no renewal).
# Falls back to GOOGLE_STATE_SECRET when unset; set it separately so rotating
# one does not invalidate the other.
ADMIN_SESSION_SECRET=
